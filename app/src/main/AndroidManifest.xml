<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.info.sayacuygulamasi">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/playstore"
        android:label="@string/app_name"
        android:roundIcon="@drawable/playstore"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Activitys.AyarActivity" />
        <activity android:name=".Activitys.VerilerActivity" />
        <activity android:name=".Activitys.OdemeActivity" />
        <activity android:name=".Activitys.SayacNo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activitys.MainActivity" />
    </application>

</manifest>